#!/usr/bin/env bash

set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"

exec uv run --script \
  "$script_dir/build_ostep.py" \
  "$script_dir/ostep-index.md" \
  -o "$script_dir/../ostep.pdf" \
  "$@"
